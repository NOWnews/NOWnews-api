
import Debug from 'debug';
const debug = Debug('NOWnews-api:api-admin:controllers:news:update');

import _ from 'lodash';

import { News } from '../../../models';
import { newsLog } from '../../../libs';

module.exports = async (req, res, next) => {
    try {
    }catch(err) {
        return next(err);
    }
};